class Scene {
    constructor(stage) {
        this.stage = stage;
        this.block_array = [];

        // Construct base scene
        
    }

    add_object(block, x, y) {
        block.se
    }
}